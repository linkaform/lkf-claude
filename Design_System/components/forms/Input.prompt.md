Single-line text field — 40px default, 48px (`size="lg"`) on auth screens.

```jsx
<Input placeholder="Usuario" size="lg" />
```

Focus paints the border blue plus a 3px 15%-opacity ring. `invalid` turns the border red; put the message in the surrounding `Field`.