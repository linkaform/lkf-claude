Centred dialog for create/confirm flows (pases, turnos, notas). Render inside a `position:relative` container.

```jsx
<Modal title="Confirmación" description="¿Cerrar el turno actual?" footer={<><Button variant="secondary">Cancelar</Button><Button variant="destructive">Cerrar turno</Button></>} />
```