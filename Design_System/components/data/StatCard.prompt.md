KPI tile — a row of three sits above notas / incidencias listings and filters them when `selectable`.

```jsx
<StatCard label="Notas abiertas" value={12} icon={<FolderOpen/>} selectable selected={sel==="abierto"} />
```