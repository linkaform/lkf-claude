Static label chip for counts, folios and categories — for record status use EstatusBadge instead.

```jsx
<Badge tone="process">F-10482</Badge>
```

Tones map to the semantic palette (success/warning/process/purple/magenta/danger) plus brand, navy, neutral and outline. `shape="square"` gives the 4px-radius folio/tag treatment.