Surface container for grouped content — white, 12px radius, hairline border.

```jsx
<Card elevation="md"><CardTitle>Detalles del turno</CardTitle><CardDescription>Caseta Norte</CardDescription></Card>
```

`interactive` raises the shadow on hover; `selected` swaps the border to the selection blue.